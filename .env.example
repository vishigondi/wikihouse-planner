# Optional local repair loop credentials.
# Keep real values in .env.local, never in committed files or browser code.
OPENAI_API_KEY=
OPENAI_REPAIR_MODEL=

# Optional marketplace/asset search provider.
SKETCHFAB_TOKEN=
